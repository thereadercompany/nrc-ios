platform :ios, '9.0'

target 'boeckler-ios' do
  use_frameworks!
  pod 'Core', :path => 'core-ios'
  pod 'Crashlytics', '~> 3.7'
  pod 'Fabric', '~> 1.6'
  pod 'Instabug', '~> 5.3'
  pod 'Reveal-iOS-SDK', :configurations => ['Debug']
end
